from setuptools import setup, find_packages

setup(
    name='helper',
    version='1.0',
    description='The helper functions with job arguments, file operations and dataframe information.',
    author='Helical',
    packages=find_packages(),
)